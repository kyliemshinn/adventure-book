import React, { ReactNode } from 'react';
import { IComponentBaseProps } from '../types';
export declare type DrawerProps = React.HTMLAttributes<HTMLDivElement> & IComponentBaseProps & {
    side: ReactNode;
    id: string;
    open?: boolean;
    mobile?: boolean;
    end?: boolean;
};
declare const Drawer: ({ children, side, open, mobile, end, id, dataTheme, className, ...props }: DrawerProps) => JSX.Element;
export default Drawer;
